import type { SupabaseClient } from "@supabase/supabase-js";

// Client Supabase genérico (browser ou server) — os services aceitam qualquer um dos dois.
export type AnySupabaseClient = SupabaseClient;
